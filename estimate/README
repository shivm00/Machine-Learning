The following a machine learning learning program that attempts to estimate the price of a house. 
The program first takes a set of training data, and calculates weights based on the data 
Then the program can estimate the price of a house using the weights it calculated based off the training data
The src folder holds the source code, called estimate.c, while the data folder holds the training data, house data, and the 
correct outputs
The txt files should be put in the command line, i.e ./estimate train.txt data.txt
The program is written in C, created in linux
